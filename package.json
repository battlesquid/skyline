{
  "name": "skyline",
  "private": true,
  "version": "1.0.0",
  "workspaces": ["packages/*"],
  "scripts": {
    "app:dev": "yarn workspace @skyline/app run dev",
    "clean:dist": "rm -rf ./packages/**/dist packages/**/tsconfig.tsbuildinfo",
    "clean:packages": "rm -rf packages/**/node_modules node_modules"
  },
  "devDependencies": {
    "prettier": "^3.3.3",
    "typescript": "^5.6.3"
  },
  "volta": {
    "node": "22.12.0",
    "yarn": "1.22.22"
  }
}
